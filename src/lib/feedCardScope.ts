/** Shared wrapper class so FeedCard styles apply outside the Feed tab. */
export const FEED_CARD_SCOPE_CLASS = "et-feed-card-scope";

export const FEED_CARD_ICON_CLASSES = {
  action: "feed-card-action-icon",
  chip: "feed-card-chip-icon",
} as const;
